<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thoughtWorks.dao.ResultReportDao">

    <select id="queryStudentsLikes" parameterType="map">
        SELECT
        department.name departmentName,
        level,
        direction.name directionName,classes.name classesName,no,student.name
        FROM t_student student
        LEFT JOIN (SELECT
        id,
        name
        FROM t_department) department ON department.id = student.department_id
        LEFT JOIN (SELECT
        id,
        name
        FROM t_direction) direction ON direction.id = student.direction_id
        LEFT JOIN (SELECT
        id,
        name
        FROM t_classes) classes ON classes.id = student.classes_id
        <where>
            <if test="departmentId!=''">
                department_id = #{departmentId}
            </if>
            <if test="level!=''">
                AND level = #{level}
            </if>
            <if test="directionId!=''">
                AND direction_id = #{directionId}
            </if>
            AND no LIKE #{no}
            and name like #{name}
        </where>

    </select>
</mapper>